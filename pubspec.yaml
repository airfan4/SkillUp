name: skillup_app
description: A Flutter app for skill-based learning pathways
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  go_router: ^12.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  dependencies:
  http: ^1.1.0  

flutter:
  assets:
    - assets/data/ 
  assets:
    - assets/images/

